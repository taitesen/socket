#ifndef _IP_INTO_INT_H
#define _IP_INTO_INT_H

unsigned int get_ip_integral_equivalent(char *);
/*
 * The function will accept IP ADDRESS as an arguments, and will return,
 * an unsigned integer which is equivalent to the parameter(IP ADDRESS).
*/
#endif //_IP_INTO_INT_H
